services:
  # ── 1. API Backend Web Service ──
  - type: web
    name: filmyfrolic-api
    env: node
    rootDir: .
    buildCommand: corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --frozen-lockfile && pnpm --filter @filmyfrolic/api build
    startCommand: node apps/api/src/index.js
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production

  # ── 2. Web Frontend Static Site ──
  - type: web
    name: filmy-frolic-new-frontend
    env: static
    rootDir: .
    buildCommand: corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --frozen-lockfile && pnpm --filter @filmyfrolic/web build
    staticPublishPath: apps/web/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
